# npm is not the project package manager. Keep this file only for read-only
# npm registry fallback commands used during diagnostics.
legacy-peer-deps=true

# Bun owns install, update, audit, script, and lockfile workflows.
# prefer-offline=true
