# 修改提交信息指南

## 需要修改的提交

1. `3f3bb18` - feat(api): add postsLight service and enhance API compatibility
   → `feat(api): 添加postsLight服务并增强API兼容性`

2. `516c43e` - refactor(api): enhance auth flow and optimize device management
   → `refactor(api): 增强认证流程并优化设备管理`

3. `fe5cbfe` - refactor(api): align endpoints with backend contract v1
   → `refactor(api): 对齐后端合约v1的API端点`

## 修改步骤

### 方法一：使用 git rebase -i（推荐）

1. 运行交互式 rebase：

```bash
git rebase -i fe5cbfe~1
```

2. 在打开的编辑器中，将前三行的 `pick` 改为 `reword`：

```
reword fe5cbfe refactor(api): align endpoints with backend contract v1
reword 516c43e refactor(api): enhance auth flow and optimize device management
reword 3f3bb18 feat(api): add postsLight service and enhance API compatibility
```

3. 保存并关闭编辑器

4. Git 会依次打开每个提交的编辑器，修改标题为：

**第一个提交（fe5cbfe）：**

```
refactor(api): 对齐后端合约v1的API端点

- 端点路径重构: 将 /auth/*, /users/me/* 迁移至 /account/* 统一账户管理
- 邮件相关端点: /verify-email, /request-password-reset 等迁移至 /email/* 命名空间
- 社区端点更新: /community/latest -> /community/feed, /community/hot -> /community/trending
- HTTP 方法标准化: PATCH -> PUT 用于资源更新, POST -> PUT 用于状态变更
- 新增服务模块: deviceService (设备管理), mediaService (媒体文件), twoFactorService (双因素认证), preferencesService (用户偏好)
- 服务重命名: sessionService -> deviceService (保留向后兼容别名)
- 数据模型调整: Post.content -> Post.description, 新增 PaginatedApiResponse.has_next/has_prev
- 收藏接口简化: favoriteService.create() 仅接受 post_id, remove() 使用 post_id 替代 favorite_id
- 错误码映射增强: 支持合约定义的 AUTH_* 和 USER_* 错误码
- 组件同步更新: 所有使用旧 API 的组件已适配新接口结构
- 向后兼容: 为 sessionService 提供别名导出，确保现有代码平滑迁移
```

**第二个提交（516c43e）：**

```
refactor(api): 增强认证流程并优化设备管理

- Auth: 简化登录/注册设备信息传递，仅发送 device_name 和 device_type
- Auth: 新增 refresh_token 支持，实现更安全的令牌刷新机制
- Auth: 登出接口支持 all_devices 参数，可一键注销所有设备
- Auth: 增强用户响应字段（full_name, is_admin, is_verified, totp_enabled, roles）
- API Client: 优化错误响应解析，支持嵌套 detail 对象结构
- API Client: 刷新令牌时携带 refresh_token 参数
- Refactor: 移除 sessionService 别名导出，统一使用 deviceService
- Refactor: 删除冗余的 sessionService.ts 文件
- UI: 导航栏优化路由监听，使用 watch 替代 watchEffect 提升性能
- UI: 路由切换时自动关闭设置面板和用户菜单
- UI: 个人资料页面新增编辑入口，直接跳转到设置页面的基本信息区块
- UI: 设置页面区块添加 ID 锚点，支持页面内导航
- UI: AnimatedIcon 在亮色主题下添加阴影效果，提升可见性
- Performance: PlatformHubBackground 限制帧率至 30fps，避免 RAF 阻塞
- Feature: 社区发帖支持标签规范化，自动去除 # 前缀和空格
- Feature: 发帖失败时自动降级重试（移除标签和引用）
- Fix: 预加载帖子详情时支持可选的评论加载
- Config: 新增 Cloudflare Pages 重定向配置文件
```

**第三个提交（3f3bb18）：**

```
feat(api): 添加postsLight服务并增强API兼容性

- Feat: 新增 postsLightService 轻量帖子列表服务，支持单一流和混合流
- Feat: 注册流程支持可选的 full_name 字段，完善用户资料
- Refactor: API 服务增加降级重试机制，提升后端兼容性
- Refactor: userService 优先使用旧端点，失败时降级到新端点
- Refactor: favoriteService 查询失败时自动移除排序参数重试
- Refactor: historyService 浏览记录支持 view_duration 和 duration 双字段
- Refactor: commentService 评论排序改为前端处理，避免后端参数不兼容
- Fix: 修复 notificationService 端点多余的斜杠
- Fix: 修复 PlatformHubBackground 语法错误（多余的闭合括号）
- Performance: PlatformHubBackground 优化渲染性能
  - 限制最大 DPR 为 1，降低渲染负载
  - 动态元素（连线和粒子）每 2 帧更新一次
  - 复用 Vector3 对象，减少内存分配
- UI: 移除收藏列表的默认排序参数，使用后端默认排序
- i18n: 新增 fullName 相关翻译（中文、英文、日文）
```

5. 完成后推送：

```bash
git push --force-with-lease origin main
```

## 注意事项

⚠️ **重要警告**：

- 这会重写 Git 历史
- 需要使用 `--force-with-lease` 强制推送
- 如果有其他协作者，请先通知他们
- 建议在修改前创建备份分支：`git branch backup-main`

## 新的提交信息规范

从现在开始，所有提交都应遵循：

- **标题**：使用中文描述（`<type>(<scope>): 中文描述`）
- **正文**：使用 `-` 作为列表符号，详细说明变更内容
