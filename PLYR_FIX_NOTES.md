# Plyr 视频容器尺寸修复说明

## 问题描述

在 PhotoSwipe 查看器中播放视频时，`plyr__poster` 和视频元素使用了视频的原始尺寸（如 720x1280），而不是自适应屏幕尺寸，导致超出容器和屏幕范围。

## 修复内容

### 1. 增强 CSS 样式优先级

- 为所有 Plyr 相关容器添加 `!important` 标记
- 添加了 `.plyr__video-wrapper` 内部容器的样式控制
- 确保 `plyr__poster` 使用绝对定位并填充整个容器

### 2. 增强 JavaScript 尺寸修复

- 在 Plyr `ready` 事件中移除固定尺寸属性
- 新增 `loadedmetadata` 事件监听，确保视频元数据加载后再次修复尺寸
- 同时修复 video、poster 和 plyr\_\_video-wrapper 三个元素

## 修复的关键点

1. **移除内联尺寸属性**：Plyr 会在初始化时设置 `width` 和 `height` 属性，需要移除
2. **强制 CSS 优先级**：使用 `!important` 确保样式不被覆盖
3. **多次修复**：在 `ready` 和 `loadedmetadata` 两个时机都进行尺寸修复
4. **object-fit: contain**：确保视频内容完整显示，不被裁切

## 测试方法

1. 打开应用并浏览包含视频的媒体内容
2. 点击视频进入 PhotoSwipe 查看器
3. 检查视频和海报是否正确适配屏幕尺寸
4. 尝试不同尺寸的视频（横屏、竖屏、不同分辨率）
5. 调整浏览器窗口大小，确保视频自适应

## 预期效果

- 视频和海报应该完整显示在屏幕内
- 不应该出现超出屏幕的情况
- 视频应该保持原始宽高比
- 在不同设备尺寸下都能正常显示
