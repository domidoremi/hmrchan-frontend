# Cloudflare Pages 重定向配置
#
# 路由由 Cloudflare Pages 自动生成的 _routes.json 控制
# 手动的 public/_routes.json 会被忽略
#
# Functions 路由（自动）：
# - /api/* → functions/api/[[path]].ts
# - /uploads/* → functions/uploads/[[path]].ts  
# - /ws → functions/ws.ts
#
# SPA 回退规则（必须）：
/* /index.html 200
