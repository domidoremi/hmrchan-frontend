# Cloudflare Pages 重定向配置
#
# 路由由 public/_routes.json 控制：
# - /api/* → functions/api/[[path]].ts
# - /uploads/* → functions/uploads/[[path]].ts  
# - /ws → functions/ws.ts
# - 其他 → 静态资源（SPA 自动回退）
#
# 此文件保留为空，避免干扰 Functions 路由
