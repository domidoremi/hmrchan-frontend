# Cloudflare Pages 重定向配置
#
# 路由优先级：
# 1. Functions 路由（由 _routes.json 控制）
# 2. _redirects 规则（按顺序匹配）
# 3. 静态文件
#
# Functions 路由（自动）：
# - /api/* → functions/api/[[path]].ts
# - /uploads/* → functions/uploads/[[path]].ts  
# - /ws → functions/ws.ts
#
# SPA 回退规则（排除 Functions 路由）：
/api/* 404
/uploads/* 404
/ws 404
/assets/* 404
/* /index.html 200
