# Cloudflare Pages HTTP headers

# 通用安全头
/*
  # HSTS - 强制 HTTPS（2 年有效期 + preload 列表）
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
  
  # 防止点击劫持
  X-Frame-Options: SAMEORIGIN
  
  # 防止 MIME 类型嗅探
  X-Content-Type-Options: nosniff
  
  # Referrer 策略
  Referrer-Policy: strict-origin-when-cross-origin
  
  # 跨域策略
  Cross-Origin-Opener-Policy: same-origin-allow-popups
  Cross-Origin-Resource-Policy: cross-origin
  Cross-Origin-Embedder-Policy: credentialless
  
  # 阻止 Flash/PDF 跨域策略文件
  X-Permitted-Cross-Domain-Policies: none
  
  # 权限策略（最小权限原则）
  # clipboard-write=(self): 分享链接复制功能
  # picture-in-picture=(self): 视频画中画
  # autoplay=(self): 帖子图片自动轮播
  Permissions-Policy: camera=(), microphone=(), geolocation=(), fullscreen=(self), payment=(), accelerometer=(), gyroscope=(), magnetometer=(), midi=(), usb=(), display-capture=(), screen-wake-lock=(), xr-spatial-tracking=(), clipboard-read=(), clipboard-write=(self), picture-in-picture=(self), autoplay=(self)
  
  # 内容安全策略
  # script-src 由 functions/_middleware.ts 动态注入 nonce 覆盖（支持 Cloudflare 注入的内联脚本）
  # 此处保留静态 CSP 作为 fallback（middleware 未执行时，如直接访问静态资源）
  # style-src: 保留 unsafe-inline（critical CSS 每次构建变化 + Vue 运行时样式）
  # media-src: 所有媒体通过 /api 代理，无需 https: 通配
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' https://static.cloudflareinsights.com https://challenges.cloudflare.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https: blob:; font-src 'self' data:; connect-src 'self' https://api.momichan.xyz https://challenges.cloudflare.com https://static.cloudflareinsights.com https://cloudflareinsights.com https://pbs.twimg.com https://i.ytimg.com; media-src 'self' blob:; object-src 'none'; worker-src 'self' blob:; frame-src 'self' https://challenges.cloudflare.com; frame-ancestors 'self'; base-uri 'self'; form-action 'self'; upgrade-insecure-requests

# HTML 页面 - 短缓存，便于快速更新
/*.html
  Cache-Control: public, max-age=0, must-revalidate

# 主入口（index.html）
/index.html
  Cache-Control: public, max-age=0, must-revalidate

# JavaScript 与 CSS 打包产物 - 长缓存（带哈希）
/assets/*.js
  Cache-Control: public, max-age=31536000, immutable

/assets/*.css
  Cache-Control: public, max-age=31536000, immutable

# 图片资源
/assets/*.(png|jpg|jpeg|gif|svg|webp|avif)
  Cache-Control: public, max-age=31536000, immutable

# 字体文件
/assets/*.(woff|woff2|ttf|otf)
  Cache-Control: public, max-age=31536000, immutable

# Service Worker - 禁止缓存
/service-worker.js
  Cache-Control: no-cache, no-store, must-revalidate

# Favicon
/favicon.ico
  Cache-Control: public, max-age=31536000, immutable
